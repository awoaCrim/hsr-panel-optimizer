# 星穹铁道 AI 面板优化（HSR Panel Optimizer）

为《崩坏：星穹铁道》构建**高度模拟官方战斗逻辑的战斗推演引擎**（v2，ADR-0007）：LLM 作为指挥者全程掌控推演（逐步决策、可回退任意行动、分支探索），面板优化为上层应用之一（工作模式：LLM 提出面板方案 → 程序用精确公式验证 → 多轮迭代收敛）。本文档定义项目领域术语；游戏规则详见 `docs/game-knowledge.md`，数据字段见 `docs/data-schema.md`。

## Language

**面板 (Stat Profile)**:
角色最终属性配置 = 基础属性 + 光锥 + 遗器主/副词条 + 行迹 + 增益，是优化对象。
_Avoid_: 面板属性、build、配装

**面板方案 (Build Proposal)**:
LLM 每轮输出的完整 JSON：四人装备选择 + 主词条 + 副词条目标 + 配速顺序 + 循环。
_Avoid_: 方案、配置

**循环 (Rotation)**:
行动序列（如 `["红A:E", "花火:E", ...]`），是验证引擎的输入，由 LLM 在方案中给出。
_Avoid_: 排轴、行动序列（口语化描述时可用，代码与文档统一用"循环"）

**推演 (Rehearsal)**:
LLM 作为指挥者在模拟环境中的完整战斗过程：逐步决策、自评、可回退、可分支探索，直到自主收敛或模拟器自动终止（AV 耗尽/全灭）。是 v2 产品的核心形态（ADR-0007）。
_Avoid_: 战斗模拟（指引擎本身）、模拟

**决策 (Decision)**:
LLM 的一次 act：主动行动（谁/用什么技能/打谁）+ 该步大招释放指令。决策空间严格对齐官方规则中玩家可控的部分（ADR-0007 D2）。
_Avoid_: 行动（指模拟器内部动作时）

**回退 (Undo)**:
撤销最近一次决策（act 级，含其全部自动连锁），回到决策前状态；仅作用于决策层，前提是推演配置未变（ADR-0007 D3/D5）。分支只能在 act 边界分叉。
_Avoid_: 撤销（口语可用）

**分支 (Branch)**:
推演中在 act 边界分叉出的替代决策路线；所有尝试过的分支保留可对比，最终由 LLM 自评选定。
_Avoid_: 路线、备选方案

**推演配置 (Setup)**:
队伍（成员+面板+光锥+星魂）+ 敌人构成 + 关卡规则 + 场地 buff + 随机 seed。配置变更 = 冻结旧推演树、开新会话；undo 永不跨配置（ADR-0007 D5）。
_Avoid_: 配置（单独用时指面板配置，易混）

**验证器 (Validator)**:
程序侧独立验证模块，v1 含伤害/速度轴/SP 三个；后续可扩展能量、约束检查。
_Avoid_: 校验器、checker

**靶场 (Target Scenario)**:
手填的战斗环境：敌人构成 + 属性 + 轮次目标（v1 为 90 级双精英 + 2T）。
_Avoid_: 敌人配置、战斗环境

**行动值 (Action Value, AV)**:
行动条的度量单位，AV = 10000 / 速度；行动条按 AV 推进，先到者行动。
_Avoid_: action value 混用单位与距离

**拉条 (Action Advance)**:
按剩余行动距离的百分比减少目标距离（花火战技 50%、知更鸟大招 100%）。
_Avoid_: 推条（推条是增加距离，方向相反，两者不可混用）

**忆灵 (Memosprite)**:
记忆命途角色召唤的独立行动单位（如记忆主的"迷迷"），有自己的行动条与技能，与角色共享能量体系之外独立结算。
_Avoid_: 召唤物、宠物

**真实伤害 (True Damage)**:
独立于常规乘区、按已造成伤害百分比追加的伤害类型（记忆主提供），直接结算不经过防御/抗性。
_Avoid_: 真伤（口语可用，代码/文档用全称）

**击破 (Break)**:
韧性条归零时触发：行动延后 25% + 击破伤害 + 击破效果。击破伤害 = 等级系数 × 属性倍率 × 击破特攻加成。
_Avoid_: 破盾

**削韧 (Toughness Damage)**:
技能对敌人韧性条的削减量，仅弱点属性攻击可削韧。

**战技点 (Skill Point, SP)**:
全队共享资源，标准基础上限 5、开局 4；角色行迹/天赋/星魂可提高上限（当前真实队伍为 9）。普攻 +1、战技通常 -1（特殊角色可不同）。SP 循环约束：任意前缀非负、循环结束不低。
_Avoid_: 技能点、行动点

**词条 (Substat)**:
遗器副词条的计量单位（暴击率 3.24%、暴伤 6.48%、攻击 4.32%、速度 2.4 等），用于量化面板预算。
_Avoid_: 副词条（指具体属性时可用）

**主指标 / 约束 / 诊断 (Score / Constraints / Diagnostics)**:
验证输出契约三件套：主指标（2T 总伤害）+ 约束清单（击杀/SP 非负/速度断点达标）+ 失败诊断（卡 SP 位置、断点差距），是 LLM 迭代的依据。
_Avoid_: 评分、报告（模糊）

**策略 (Policy)**:
角色的战斗决策规则集（大招开关、回路连打上限、战技预算、拉条目标），模拟器按规则实时决策而非消费写死的行动序列。决策是真实优化空间：程序枚举参数空间找当前面板下的最优策略（策略搜索），LLM 可在此基础上微调。
_Avoid_: 循环、轴（指写死的行动序列时）

**策略搜索 (Policy Search)**:
在当前面板下枚举决策参数（连打上限×战技预算×大招开关）求 2T 伤害最优策略的确定性搜索。与 LLM 迭代配合：搜索保证"面板基于最优打法验证"，LLM 专注面板与策略微调。

## Rules

- **Be opinionated.** 术语定义以本项目使用为准（如"拉条"与"推条"方向相反，不可混用）。
- **中英对应.** 中文术语用于文档与 LLM 交互，英文用于代码标识符（如 `ActionAdvance`、`Memosprite`、`TrueDamage`、`TargetScenario`）。
- 数据源相关术语（StarRailRes / TurnBasedGameData / AvatarSkillConfig）是外部系统名，见 `docs/data-schema.md`，不收入本表。
