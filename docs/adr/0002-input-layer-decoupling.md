# 0002 输入层与引擎解耦：静态 JSON 先行，米游社适配器后补

装备池/面板输入（"角色当前属性"）最终希望从米游社接口一键读取（候选库：PaiGramTeam/SIMNet、seriaati/genshin.py，均活跃维护）。但该能力引入 cookie 配置、第三方库、接口变动风险。决策：输入层定义为稳定 JSON schema，v1 用手填/静态 JSON 跑通闭环，米游社导入器作为同一 schema 的适配器后补接入——引擎不感知数据来源，闭环不阻塞，适配器随时可插。
