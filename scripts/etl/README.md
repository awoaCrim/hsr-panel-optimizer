# scripts/etl — ETL 提取脚本（占位）

本目录用于后续阶段的数据提取脚本（Python）：

- 从 StarRailRes（index_min/cn）提取 → `data/player_data/`
- 从 TurnBasedGameData（ExcelOutput）提取 → `data/battle_data/`
- TextMap 翻译 → `data/textmap/`

字段清单与输出 schema 见 `docs/data-schema.md` 第四节。
